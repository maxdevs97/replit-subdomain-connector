{
  "name": "replit-subdomain-connector",
  "version": "1.0.0",
  "description": "Connect Replit projects to sher.dev subdomains",
  "scripts": {
    "install-all": "cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/maxdevs97/replit-subdomain-connector.git"
  },
  "author": "Sher Agency",
  "license": "PROPRIETARY",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
